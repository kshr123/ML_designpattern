# uv パッケージマネージャー完全ガイド

## 📚 目次

1. [uvとは](#uvとは)
2. [なぜuvを使うのか](#なぜuvを使うのか)
3. [uvの基本コマンド](#uvの基本コマンド)
4. [pyproject.tomlとの連携](#pyprojecttomlとの連携)
5. [uvとDockerの違い](#uvとdockerの違い)
6. [実践例](#実践例)
7. [トラブルシューティング](#トラブルシューティング)

---

## uvとは

**uv**は、Rustで書かれた**超高速なPythonパッケージマネージャー**です。

### 特徴

- ⚡️ **圧倒的に高速**: pipの10-100倍速い
- 🔒 **依存関係の解決が正確**: 複雑な依存関係も確実に解決
- 🎯 **モダン**: pyproject.tomlネイティブサポート
- 🔄 **pip互換**: pipコマンドと同じように使える
- 📦 **オールインワン**: 仮想環境作成からパッケージ管理まで

### 比較

| ツール | 速度 | 依存関係解決 | pyproject.toml | 学習コスト |
|--------|------|-------------|----------------|-----------|
| **uv** | ⚡️⚡️⚡️ | ✅ 正確 | ✅ ネイティブ | 低 |
| pip | 🐌 | ⚠️ 問題あり | ⚠️ 間接的 | 低 |
| poetry | 🐌🐌 | ✅ 正確 | ✅ ネイティブ | 高 |
| pipenv | 🐌 | ⚠️ 遅い | ❌ | 中 |

---

## なぜuvを使うのか

### 理由1: 圧倒的な速度

```bash
# 実際のベンチマーク（22パッケージのインストール）
uv pip install:    17ms  ⚡️
pip install:     8000ms  🐌 (470倍遅い)
```

### 理由2: 依存関係の完全な再現性

**問題のあるケース（pipの場合）:**
```bash
# 開発者Aのマシン
pip install fastapi  # → fastapi 0.115.0 + 依存パッケージ

# 開発者Bのマシン（数ヶ月後）
pip install fastapi  # → fastapi 0.120.0 + 異なる依存パッケージ
# → バージョン違いで動かない！
```

**uvの場合:**
```toml
# pyproject.toml に明記
[project]
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
```

```bash
# どのマシンでも、いつ実行しても同じ
uv pip install -e .  # → pyproject.tomlから確実にインストール
```

### 理由3: pyproject.tomlで一元管理

**古い方法（requirements.txt）:**
```
requirements.txt      # 本番環境の依存関係
requirements-dev.txt  # 開発環境の依存関係
setup.py              # パッケージメタデータ
```
→ ファイルが分散、管理が煩雑

**uvの方法（pyproject.toml）:**
```toml
[project]
name = "my-project"
version = "0.1.0"
dependencies = [...]           # 本番環境

[project.optional-dependencies]
dev = [...]                    # 開発環境

[tool.pytest.ini_options]      # pytestの設定
[tool.black]                   # blackの設定
[tool.mypy]                    # mypyの設定
```
→ **1ファイルで全て管理**

---

## uvの基本コマンド

### インストール

```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Homebrew（推奨）
brew install uv

# バージョン確認
uv --version
```

### 仮想環境の管理

```bash
# 仮想環境を作成
uv venv

# 仮想環境を作成（Pythonバージョン指定）
uv venv --python 3.13

# 仮想環境を有効化
source .venv/bin/activate       # macOS/Linux
.venv\Scripts\activate          # Windows

# 仮想環境を無効化
deactivate
```

### パッケージのインストール

```bash
# 単一パッケージをインストール
uv pip install fastapi

# バージョン指定でインストール
uv pip install "fastapi>=0.115.0"

# 複数パッケージをインストール
uv pip install fastapi uvicorn sqlalchemy

# pyproject.tomlからインストール
uv pip install -e .

# pyproject.tomlから開発用依存関係もインストール
uv pip install -e ".[dev]"

# requirements.txtからインストール（互換性）
uv pip install -r requirements.txt
```

### パッケージの管理

```bash
# インストール済みパッケージの一覧
uv pip list

# パッケージの詳細情報
uv pip show fastapi

# パッケージのアンインストール
uv pip uninstall fastapi

# 全パッケージのアンインストール
uv pip freeze | uv pip uninstall -r /dev/stdin
```

### 依存関係のエクスポート

```bash
# requirements.txtを生成（互換性のため）
uv pip freeze > requirements.txt

# pyproject.tomlから生成
uv pip compile pyproject.toml -o requirements.txt
```

---

## pyproject.tomlとの連携

### 基本構造

```toml
[project]
name = "model-db"
version = "0.1.0"
description = "Model Database Management System"
requires-python = ">=3.13"

# 本番環境の依存関係（必須）
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "pydantic>=2.9.0",
]

# 開発環境の依存関係（オプション）
[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=5.0.0",
    "black>=24.8.0",
    "ruff>=0.6.0",
    "mypy>=1.11.0",
]

# ビルドシステム
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

### バージョン指定の方法

```toml
dependencies = [
    "fastapi>=0.115.0",      # 0.115.0以上
    "uvicorn==0.30.0",       # 0.30.0のみ
    "sqlalchemy~=2.0.0",     # 2.0.x系（2.1.0は不可）
    "pydantic>=2.0,<3.0",    # 2.0以上3.0未満
    "requests",              # 最新版
]
```

### インストールコマンド

```bash
# 本番環境の依存関係のみ
uv pip install -e .

# 開発環境の依存関係も含める
uv pip install -e ".[dev]"

# 複数のオプショナル依存関係
uv pip install -e ".[dev,test,docs]"
```

---

## uvとDockerの違い

### 管理する範囲

```
┌─────────────────────────────────────┐
│             Docker                  │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │         OS（Ubuntu等）          │ │
│ ├─────────────────────────────────┤ │
│ │      システムパッケージ         │ │
│ ├─────────────────────────────────┤ │
│ │      Python + パッケージ        │ │
│ ├─────────────────────────────────┤ │
│ │    データベース（PostgreSQL）   │ │
│ ├─────────────────────────────────┤ │
│ │      ネットワーク設定           │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│              uv                     │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │     Pythonパッケージのみ        │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 使い分け

| シナリオ | uv | Docker | 理由 |
|---------|-------|--------|------|
| **学習・個人開発** | ✅ | ❌ | シンプルで十分 |
| **チーム開発（Python環境のみ）** | ✅ | ❌ | uvで統一可能 |
| **複数サービス連携** | ❌ | ✅ | DB、Redis等が必要 |
| **本番環境デプロイ** | ❌ | ✅ | 環境の完全統一 |
| **CI/CD** | ⚠️ | ✅ | Dockerが標準的 |

### 例：Model DBプロジェクト

**uvで十分な理由:**
```
✅ FastAPIアプリのみ
✅ SQLite（Pythonに標準搭載）
✅ 外部サービス不要
✅ 学習目的

→ uvで完全に管理可能
```

**Dockerが必要になる場合:**
```
❌ PostgreSQLを使う
❌ Redisを使う
❌ 複数のマイクロサービス
❌ Kubernetesへデプロイ

→ Docker Composeで管理
```

---

## 実践例

### 例1: 新しいプロジェクトのセットアップ

```bash
# 1. プロジェクトディレクトリ作成
mkdir my_project
cd my_project

# 2. Pythonバージョン指定
echo "3.13" > .python-version

# 3. pyproject.toml作成
cat << 'EOF' > pyproject.toml
[project]
name = "my-project"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "black>=24.8.0",
]
EOF

# 4. 仮想環境作成
uv venv

# 5. 仮想環境有効化
source .venv/bin/activate

# 6. 依存関係インストール
uv pip install -e ".[dev]"

# 7. 確認
python -c "import fastapi; print('Success!')"
```

### 例2: 既存プロジェクトのクローン

```bash
# 1. リポジトリをクローン
git clone https://github.com/user/project.git
cd project

# 2. 仮想環境作成
uv venv

# 3. 仮想環境有効化
source .venv/bin/activate

# 4. 依存関係インストール
uv pip install -e ".[dev]"

# → pyproject.tomlがあれば、全ての依存関係が自動で揃う
```

### 例3: 新しいパッケージの追加

```bash
# 1. パッケージをインストール
uv pip install requests

# 2. pyproject.tomlに追加
# dependencies = [
#     "fastapi>=0.115.0",
#     "requests>=2.31.0",  # ← 追加
# ]

# 3. 他の開発者は
uv pip install -e .  # → requestsも自動でインストールされる
```

---

## トラブルシューティング

### 問題1: uv pip install が遅い

**原因**: キャッシュが破損している

**解決方法**:
```bash
# キャッシュをクリア
uv cache clean

# 再インストール
uv pip install -e .
```

### 問題2: パッケージのバージョンが競合する

**症状**:
```
error: Multiple packages match the requirement
```

**解決方法**:
```toml
# pyproject.tomlでバージョンを明示的に指定
dependencies = [
    "package-a>=1.0.0,<2.0.0",
    "package-b>=2.0.0",
]
```

### 問題3: 仮想環境が有効化されない

**確認方法**:
```bash
which python
# → /Users/you/project/.venv/bin/python であればOK
# → /usr/bin/python なら仮想環境が有効化されていない
```

**解決方法**:
```bash
# 仮想環境を明示的に有効化
source .venv/bin/activate

# プロンプトに (.venv) が表示されることを確認
```

### 問題4: pyproject.tomlが認識されない

**症状**:
```bash
uv pip install -e .
# → error: Could not find pyproject.toml
```

**解決方法**:
```bash
# 現在のディレクトリを確認
pwd

# pyproject.tomlがあるディレクトリに移動
cd path/to/project

# 再実行
uv pip install -e .
```

---

## 📚 参考リンク

- [uv公式ドキュメント](https://github.com/astral-sh/uv)
- [pyproject.toml仕様](https://packaging.python.org/en/latest/specifications/pyproject-toml/)
- [PEP 621 (pyproject.tomlの標準化)](https://peps.python.org/pep-0621/)

---

## 💡 ベストプラクティス

### ✅ やるべきこと

1. **pyproject.tomlで依存関係を管理する**
   - requirements.txtは使わない（互換性のため生成するのはOK）

2. **バージョンは適度に制約する**
   ```toml
   "fastapi>=0.115.0"  # ✅ Good: マイナーアップデート許可
   "fastapi"           # ❌ Bad: バージョン固定なし
   "fastapi==0.115.0"  # ⚠️ OK: 厳密だが更新が面倒
   ```

3. **開発用依存関係を分ける**
   ```toml
   [project.optional-dependencies]
   dev = ["pytest", "black", "mypy"]
   ```

4. **.venvはgitignoreに追加**
   ```gitignore
   .venv/
   *.pyc
   __pycache__/
   ```

### ❌ やってはいけないこと

1. **グローバル環境にパッケージをインストール**
   ```bash
   # ❌ Bad
   uv pip install fastapi  # 仮想環境外で実行

   # ✅ Good
   uv venv
   source .venv/bin/activate
   uv pip install fastapi
   ```

2. **pyproject.tomlとコードで依存関係が一致しない**
   ```python
   # コードで使っているのに pyproject.toml に書いてない
   import requests  # ❌ Bad
   ```

3. **仮想環境をコミットする**
   ```bash
   git add .venv/  # ❌ Bad
   ```

---

## 📊 まとめ

### uvを使う理由（再確認）

| 項目 | 説明 |
|------|------|
| **速度** | pipの10-100倍速い |
| **再現性** | pyproject.tomlで確実に同じ環境を作れる |
| **シンプル** | 1つのファイルで管理完結 |
| **モダン** | Python公式の標準仕様に準拠 |

### このプロジェクトでの使い方

```bash
# 新しいパターンを始める時
cd my_implementations/ch{N}__{pattern}
echo "3.13" > .python-version
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"

# 開発中
uv pip install <new-package>  # 新しいパッケージ追加

# テスト実行
pytest

# コードフォーマット
black src/ tests/
```

これでuvの完全ガイドは完成です！
